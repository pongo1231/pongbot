#include "EntityData.h"

namespace EntityDataFetcher {
	SendProp *FetchFromEdict(edict_t *edict, int data) {
		// TODO!!!!!!
		//edict->GetNetworkable()->GetServerClass()->m_pTable->GetProp(data)->
		return nullptr;
	}
}